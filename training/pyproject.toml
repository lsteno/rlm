[project]
name = "rlms-training"
version = "0.0.0"
description = "Training utilities for rlms."
requires-python = ">=3.11"
dependencies = [
    "pyarrow>=23.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["training"]